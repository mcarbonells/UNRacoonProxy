# Reverse proxy

This is a reverse proxy component from https to https on Pinart system.

## Usage

in order to start the server copy the proper certificates on the directory `develpment` (`server.cert` and `server.key`). Then run:

```bash
docker-compose up --build
```

